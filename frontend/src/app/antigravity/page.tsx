'use client';

import AntigravityShowcase from '@/components/special/AntigravityShowcase';

export default function AntigravityPage() {
    return (
        <main className="bg-antigravity-deep min-h-screen">
            <AntigravityShowcase />
        </main>
    );
}
